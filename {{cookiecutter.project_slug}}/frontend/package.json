{
	"name": "frontend",
	"version": "0.1.0",
	"private": true,
	"scripts": {
        "dev": "vite dev --port 3000",
        "dev-directly": "vite --mode mobile dev --host --port 3000",
        "build": "vite build",
        "package": "svelte-kit package",
        "preview": "vite build && vite preview --port 3000",
        "preview-fast": "vite preview --port 3000",
		"test": "playwright test",
		"test:unit": "vitest",
        "check-updates": "ncu",
        "auto-update": "ncu -u"
	},
	"devDependencies": {
		"@playwright/test": "^1.29.1",
		"vitest": "^0.26.3"
    },
	"type": "module",
    "dependencies": {
		"@sveltejs/adapter-auto": "^1.0.0",
        "@sveltejs/adapter-node": "^1.0.0",
		"@sveltejs/kit": "^1.0.1",
		"svelte": "^3.55.0",
		"vite": "^4.0.3",
        "cookie": "^0.5.0",
        "html-minifier": "^4.0.0"
    }
}
